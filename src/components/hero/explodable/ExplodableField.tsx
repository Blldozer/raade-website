
import React from 'react';

/**
 * ExplodableField Component
 * 
 * Previously displayed interactive building blocks in the hero area.
 * Now an empty component as per user request to remove the blocks.
 */
const ExplodableField = () => {
  return (
    <div className="absolute inset-0 z-20">
      {/* Blocks removed as requested */}
    </div>
  );
};

export default ExplodableField;
