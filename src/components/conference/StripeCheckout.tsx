
// Re-export the Stripe checkout component from its new location
// This ensures backward compatibility with existing imports
import StripeCheckout from "./payment/checkout/StripeCheckout";
export default StripeCheckout;
